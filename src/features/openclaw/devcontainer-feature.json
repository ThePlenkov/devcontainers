{
  "id": "openclaw",
  "version": "1.0.0",
  "name": "OpenClaw",
  "description": "Install OpenClaw — a personal AI assistant CLI with TUI, messaging channels, multi-agent runtimes, and MCP tool bridge.",
  "documentationURL": "https://github.com/openclaw/openclaw",
  "keywords": ["ai","openclaw","agent","cli","coding","assistant"],
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Version to install"
    },
    "shareConfig": {
      "type": "boolean",
      "default": false,
      "description": "When true, symlink the agent config dir to the shared AGENT_CONFIG_DIR so config persists across container rebuilds and is shared with other agents"
    }
  },
  "dependsOn": {
    "ghcr.io/docker-x/devcontainers/agent-config": {}
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils","ghcr.io/devcontainers/features/node","ghcr.io/docker-x/devcontainers/agent-config"
  ]
}
